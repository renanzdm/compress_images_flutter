name: compress_images_flutter
description: Plugin Flutter for compress image.
version: 0.0.2
homepage: https://github.com/renanzdm/compress_images_flutter
environment:
  sdk: ">=2.17.5 <3.0.0"
  flutter: ">=2.5.0"

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  plugin:
    platforms:
      android:
        package: com.example.compress_images_flutter
        pluginClass: CompressImagesFlutterPlugin
      ios:
        pluginClass: CompressImagesFlutterPlugin


